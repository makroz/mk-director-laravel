<?php

declare(strict_types=1);

namespace App\Modules\{{ModuleName}}\Http\Resources;

use Mk\Director\Http\Resources\MkRoleResource;

/**
 * Resource para Role del módulo {{ModuleName}}.
 *
 * Generado por `php artisan mk:make:auth-user {{ModuleName}} --with-crud` (R-PKG-014).
 *
 * EL SHAPE VIVE EN EL PAQUETE, NO ACÁ — ver {@see MkRoleResource}, y el
 * docblock de `ability-resource.stub` por la razón completa.
 *
 * Lo que hereda y conviene no perder de vista:
 *  - `abilities` es CONDICIONAL (`whenLoaded`): sale sólo si el controller
 *    hizo `->with('abilities')`. Si se resolviera siempre, el índice de roles
 *    dispararía un N+1.
 *  - `description` (nullable) la agrega la migración
 *    `add_description_to_roles_table`. Revierte F10-B07, que la sacó cuando la
 *    columna todavía no existía.
 *
 * 🔴 Si sobreescribís `toArray()`, AGREGÁ claves. Renombrar o sacar una de las
 * del paquete es breaking para los fronts que ya la consumen.
 */
class RoleResource extends MkRoleResource
{
}
